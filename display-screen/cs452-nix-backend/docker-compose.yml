version: '3.8'

services:
  darcos:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ENVIRONMENT=production
      - DJANGO_SECRET_KEY=yoursecretkeyhere
      - PORT=8000
    volumes:
      - ./backend/db.sqlite3:/app/backend/db.sqlite3
    restart: always
